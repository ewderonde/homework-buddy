services:

# Forms
    app.form.user:
        class: AppBundle\Form\UserType
        arguments:
            - '@translator'
        tags:
            - { name: form.type }

    app.form.task:
        class: AppBundle\Form\TaskType
        arguments:
            - '@translator'
        tags:
            - { name: form.type }