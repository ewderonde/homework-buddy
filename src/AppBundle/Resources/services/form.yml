services:

# Forms
    app.form.user:
        class: AppBundle\Form\UserType
        arguments:
            - '@translator'
        tags:
            - { name: form.type }

    app.form.task:
        class: AppBundle\Form\TaskType
        arguments:
            - '@translator'
        tags:
            - { name: form.type }

    app.form.subject:
        class: AppBundle\Form\SubjectType
        arguments:
            - '@translator'
        tags:
            - { name: form.type }

    app.form.subject_grade:
        class: AppBundle\Form\SubjectGradeType
        arguments:
            - '@translator'
        tags:
            - { name: form.type }

    app.form.grade:
        class: AppBundle\Form\GradeType
        arguments:
            - '@translator'
        tags:
            - { name: form.type }